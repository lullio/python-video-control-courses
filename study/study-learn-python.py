# variables
message = "hello, python!"
number = 42

# data types
string = "text string"
integer = 42
float_number = 3.14
boolean = True
list = [1,2,3,4,5,6]

# if else
if string == "text string":
   print(string)
else:
   print("não é text string")
   
# for loop
for item in list:
   print(item)

# while loop
while array.length > 1:
   array.
   
# list methods
# Lists are the most commonly used data structure in Python that can be compared to arrays in other languages

# Adding Elements:
list.append(7) # adds element 7 to the end of the list
list.extend()
list.insert(i, index) # Insert element 'x' at index 'i'

# Removing elements
list.pop([i]) # remove and returns the element at index 'i'
list.remove(x) # remove the first occurrence of element 'x'
list.clear() # remove all elements from the list

# Acessing Elements:
list[i] # access elements by index
list[start:end] # access slice the list

# Searching and Counting
index()
